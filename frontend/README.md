# Farely Mobile App

## Setup
```bash
npm install
npm start
```

## Environment

Create `.env` in `frontend/` for Stripe-enabled payment UI:

```bash
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
```